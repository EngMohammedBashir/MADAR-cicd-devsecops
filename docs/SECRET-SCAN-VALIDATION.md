# Validation Boundary

A configured scanner is IMPLEMENTED; a successful PR run makes the clean-repository scan VALIDATED; a deliberately failing synthetic fixture run is required to validate blocking behavior. These claims remain separate.
