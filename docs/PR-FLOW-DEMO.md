# Pull Request CI Flow Demo

This file exists only to demonstrate the Phase 06 pull-request validation flow without changing application behavior.

Expected flow:

`feature branch -> pull request -> GitHub Actions CI -> review -> merge to main`

No AWS resources are created by this demo.
